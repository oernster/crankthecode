services:
  - type: web
    name: crankthecode
    env: python
    buildCommand: "pip install -r requirements.txt && python -m app.assets.build_static"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
